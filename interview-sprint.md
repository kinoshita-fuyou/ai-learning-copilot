# 面试冲刺包

> 基于 EvidenceQA 与 ReportFlow 的真实实现整理。用法：每天 1 小时，挑 3 道题
> 限时 3 分钟口头回答，再对照示范答案，最后用自己的话复述一遍。目标是建立
> "项目事实库"——任何问题都能回到项目上讲，而不是背答案。

## 1. 自我介绍（60 秒版）

> 我叫【姓名】，计算机专业在读。过去十天我独立完成了两个 AI 应用项目，现在
> 系统性地补了 RAG 和 Agent 这两条主线。第一个是 EvidenceQA，一个可溯源的企业
> 知识库问答系统：上传文档后自动切分向量化，提问时先检索相关原文片段，再生成
> 带引用来源的回答，我用 Recall@K 和 MRR 建了评测体系，21 道跨文档评测题
> 混合检索都能达到 1.0（纯向量基线 MRR 0.9683）。第二个是 ReportFlow，一个把
> 自然语言任务变成结构化报告的 Agent，核心是工具调用、结构化输出校验和失败
> 降级。两个项目都做到离线可演示、58 个
> 自动化测试、CI 自动回归。我投贵司的 AI 应用开发岗位，是因为……【结合岗位说
> 一句，例如：你们的产品需要把 LLM 落地到具体业务流程，这正是我这两个项目
> 覆盖的能力。】

## 2. 技术面模拟（五组 20 题）

### A 组：RAG 与检索（EvidenceQA）

**A1. 讲一下 EvidenceQA 的整体流程。**

两条链路。离线：上传文档 → 四重校验（类型/编码/空文件/大小）→ 清洗 → 段落优先
切分（保留字符范围、重叠上下文）→ 特征哈希 Embedding → 存 SQLite。在线：问题
Embedding → 与所有 chunk 算余弦相似度 + BM25 关键词分（alpha 融合）→ Top-K
→ 拼 prompt 生成回答 → 返回
答案 + 引用来源。另外一条评测链路：21 道题跑 Recall@K / MRR / 延迟。

追问：**切分为什么保留字符范围？** 让"回答来自原文哪里"精确到位置，是引用溯源
和高亮的基础，也方便审计。**重叠上下文解决什么？** 避免把一句话从中间切断，
丢失语义边界。

**A2. 为什么自研 Embedding？特征哈希是什么？**

演示要离线、确定、可复现。做法：token 化（英文单词 + 中文单字 + 双字元）→ 每个
token 哈希映射到 512 维 → L2 归一化。本质是词袋模型，抓关键词重叠，但不懂语义，
所以接口层与云端 Embedding 对齐，换 BGE/OpenAI 只替换实现类。

追问：**接真实 Embedding 后流程哪里要改？** 只改 `HashingEmbedder`，检索、
评测、回答代码不动；用同一套 20 题评测集重跑基线。

**A3. 怎么保证检索质量？**

先有评测再调优。评测集是"问题 + 目标文档"，算 Recall@K（找不找得到）和 MRR
（排得靠不靠前），外加平均延迟。局限我也清楚：它只评估检索，不评估回答质量，
所以下一步要加引用忠实度评测和 LLM-as-judge。

追问：**MRR 怎么算？** 目标文档第一次出现位置的倒数，求平均。**数据量大了
怎么办？** 现在全表扫描，百万级 chunk 需要 HNSW/FAISS，或 BM25 混合检索 + 重排。

**A4. 怎么防 LLM 幻觉？**

三层：prompt 约束"只依据上下文回答，信息不足就说明"；回答必须带引用来源可核查；
检索为空时明确返回"未找到相关文档"。诚实讲，prompt 只能降低不能消除，生产要加
评测和人工抽查。

### B 组：Agent 与工具调用（ReportFlow）

**B1. 工具调用循环怎么设计的？**

模型收到任务后自主决定调哪些工具（query_sales 等），每次工具结果以 tool message
回传，模型观察后再决定下一步，最后强制调用 generate_report 提交结构化报告。
循环有轮数上限防死循环；结构化输出用 Pydantic 校验，失败把错误回传给模型重试
一次。

追问：**为什么不直接让模型输出 JSON？** 强制工具调用比自由文本更可控：参数走
JSON Schema、结果有契约校验、失败有重试路径，是工程上更稳的结构化输出方式。

**B2. Agent 和 Workflow 为什么分离？**

Agent 只负责两个决策点（规划调什么工具、生成什么报告），重试、依赖解析、降级、
统计全在 workflow 里。这样可以用规则 Agent 测 workflow 的正确性，再换 LLM Agent
验证决策质量，两条路径共用同一个执行器。

追问：**工具间依赖怎么处理？** 规则路径用 `{"$ref": "query_sales"}` 引用前序
输出；LLM 路径模型自己传参；执行器按顺序解析。

**B3. 失败处理具体有哪些层？**

工具失败重试一次；依赖缺失（前序工具失败）记录原因不崩溃；生成异常自动降级到
规则 Agent；报告带 `degraded`/`fallback` 标记和 tool_stats，失败可观测。演示时
用 `simulate_failure` 注入失败，展示"降级而不是崩溃"。

**B4. 规则 Agent 和 LLM Agent 为什么能共用一套测试？**

两者实现同一个 `ReportAgent` 协议，workflow 不感知具体实现；测试用
`REPORTFLOW_AGENT=rule` 强制离线，所以 CI 和演示都不依赖网络与 Key。

### C 组：Python 与后端工程

**C1. 为什么选 FastAPI？**

类型注解驱动的 Pydantic 校验、自动 OpenAPI 文档、异步支持，且和 pytest 生态
配合好，一个 TestClient 就能测全接口。

**C2. 错误处理怎么设计的？**

上传：415（类型）/422（空文件、编码）/413（大小）；资源不存在 404；LLM 提供方
异常 502 带原因。前端直接把 detail 展示给用户，前后端约定由测试锁定。

**C3. 怎么保证代码可测？**

每个测试用临时 SQLite 隔离数据；conftest 强制离线 Provider/Agent，环境无关；
分层测：单测（切分/Embedding/工具）、接口测、工作流测（降级/回退）、UI 测。
58 个用例秒级跑完。

**C4. 数据层设计上有什么取舍？**

正文、chunk、embedding 分表存，生命周期不同可单独重建；chunk 保留字符范围；
旧库缺列自动迁移回填。规模小时 SQLite 足够，不需要引入外部数据库。

### D 组：数据与评测

**D1. Recall@K 和 MRR 分别衡量什么？为什么两个都要？**

Recall@K 管"找不找得到"（召回），MRR 管"排得靠不靠前"（排序）。只用召回不看
位置会漏掉排序问题；只用 MRR 可能掩盖某些题完全没召回。

**D2. 评测集怎么来的？有什么局限？**

根据演示文档人工构造"问题 + 目标文档"对，覆盖三份文档。局限：样本少、只测检索
不测回答、没有跨领域泛化验证。改进方向：扩样本、加回答质量评测、做 bad-case
分析。

**D3. 延迟怎么测？口径是什么？**

每次 `search_chunks` 用 `perf_counter` 计时求平均。口径是单次检索延迟，不含
LLM 生成和网络。所以报告"约 0.4ms"时一定说明这是本地特征哈希 + 小语料的基线。

**D4. 如果检索效果差，你会怎么排查？**

先看 bad-case：是没召回还是排序差？再分环节定位：切分（语义被切碎？）、
Embedding（词袋抓不到同义表达？）、检索（top_k 太小？）。每一步改动用评测集
对比，而不是凭感觉。

### E 组：部署与工程化

**E1. 怎么部署的？**

Docker Compose 一条命令；`EVIDENCEQA_DB_PATH` 指向命名卷，容器删除数据不丢；
healthcheck 轮询 /health；回答 Provider、模型、Key 全走环境变量，同一镜像不同
环境只改配置。

**E2. CI 怎么设计？**

两个项目矩阵并行：push 到 main 或 PR 时，各自装依赖跑 pytest；`paths` 过滤，
只改一个项目不会白跑另一个；测试环境变量强制离线，避免 CI 因网络抖动挂掉。

**E3. 本地开发和生产的差异你怎么处理？**

本地 uvicorn --reload + 默认离线模式；生产 Docker + 可选 LLM 模式；数据库路径、
Provider、模型都是环境变量，代码不做环境分支。

**E4. 这个项目上线前还缺什么？**

鉴权与多租户（谁能搜到什么文档）、真实 Embedding + 向量索引、回答质量评测、
流式输出、日志与监控、PDF/Word 解析。我按这个顺序推进，每一步用评测集验证。

## 3. 行为面模拟（STAR 示范）

**B1. 讲一个你独立解决技术难题的经历。**

情境：EvidenceQA 最初用云端 Embedding，演示依赖网络和 Key。任务：让系统断网
可演示、测试可复现。行动：改为特征哈希确定性 Embedding，接口层与云端对齐，
并把 Provider 抽象成可替换实现。结果：离线全链路可跑，58 个测试环境无关，
换真实 Embedding 只改一个类。

**B2. 项目里哪次决策你后来觉得可以做得更好？**

诚实版本：评测集是我"想当然"构造的，有一题"客户信息如何传输"在加入第二份
文档后排名掉了，说明关键词重叠的题容易误命中。改进：我在学习路径里规划了
真实 Embedding + 混合检索，并用同一套评测集重测。这题展示"有反思 + 有行动"。

**B3. 为什么你是非科班/双非，凭什么跟科班竞争？**

不说"努力"这种空话，讲证据：十天两个完整项目、58 个自动化测试、评测指标可
验证；我用"先建评测再调优"的思路做工程，而不是堆功能。双非说明不了能力，
项目能。

**B4. 你怎么评估自己是否适合这个岗位？**

把岗位 JD 拆成能力项，对照项目：RAG → EvidenceQA，Agent 流程 → ReportFlow，
工程化 → Docker/CI/测试。有缺口就直说并给补法（比如多租户权限我在改进清单里）。

**B5-B8（速答要点）**

- **为什么投我们**：结合 JD 说 1-2 个具体能力匹配点 + 1 个公司业务点
- **职业规划**：1-2 年做透 AI 应用落地（RAG/Agent），再往架构方向走
- **能接受加班吗**：项目冲刺期可以，更希望靠效率而不是耗时长
- **还有什么问题**：见下方反问清单，选 2-3 个

## 4. 反问清单（选 2-3 个）

1. 团队现在把 LLM 落地在哪些业务场景？卡点通常是哪类？
2. 你们评价实习生的标准是什么？三个月内做到什么样算合格？
3. 这个岗位和算法/工程团队怎么协作？
4. 团队对 RAG/Agent 的评测体系怎么做的？（把话题引到你的优势）
5. 如果我入职，前两周最希望我先熟悉什么？

## 5. 被问住时的应对框架

1. 承认 + 复述："这个问题我之前没深入想过，我的理解是……"
2. 回到已知："虽然没直接做过，但我项目的类似环节是……（切分/评测/降级）"
3. 给方向："如果让我做，我会先……（用评测集验证/先看 bad-case）"
4. 切忌硬编数字或概念，面试官更看重诚实和思路。

## 6. 投递话术与求职信模板

**BOSS 直聘/拉勾打招呼（第一句）**

> 您好，我是【学校】计算机专业的学生，独立完成了两个 AI 应用项目：RAG 知识库
> 问答系统（Recall@K=1.0，38 个测试全绿，Docker 部署）和结构化报告 Agent
> （工具调用 + 失败降级）。看到贵司【岗位】在招，我的项目正好覆盖 RAG/Agent
> 落地，方便的话想投递一份简历，谢谢！

**求职信（邮件/网申版）**

> 尊敬的招聘负责人：
>
> 您好！我是【学校】计算机专业学生【姓名】，应聘【岗位】。我的技术背景围绕
> AI 应用落地，过去十天独立完成了两个可演示项目：EvidenceQA 可溯源知识库问答
> 系统，覆盖文档接入、向量检索、RAG 引用溯源与 Recall@K/MRR 评测；ReportFlow
> 结构化报告 Agent，实现工具调用、结构化输出校验与失败降级。两个项目共 58 个
> 自动化测试，GitHub Actions CI 自动回归，支持 Docker 一键部署与断网演示。
>
> 我习惯"先建评测再调优"的工程方式，注重可验证、可解释的交付。期待有机会
> 加入贵司，把 LLM 能力落地到真实业务场景。
>
> 此致敬礼
> 【姓名】 【电话】 【作品集链接】

## 7. 面试前一周每日清单

- 每天：3 道技术题限时口头回答 + 1 道行为题 STAR 练习（共约 1 小时）
- 第 1-2 天：过完 A、B 两组（RAG + Agent，你的主战场）
- 第 3-4 天：过完 C、D、E 组，把答不顺的题标记重练
- 第 5 天：完整演练 5 分钟项目演示（`demo.sh`），计时并录下来回看
- 第 6 天：自我介绍 60 秒/90 秒各录一遍 + 行为面 STAR 全过
- 第 7 天：过 [EvidenceQA 自检清单](evidenceqa/interview/06-launch-checklist.md)，
  早睡
